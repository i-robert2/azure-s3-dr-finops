# DR drill — 2026-07-03 10:29

Fault model: primary cluster `notes` deployment scaled to 0 (ingress fails the
Front Door /api/healthz probe). Front Door endpoint: `endpoint-s3-bggfb2h8djgsdner.z03.azurefd.net`.

| Metric | Target (SLO) | Measured |
|---|---|---|
| RTO — Front Door failover to DR | <= 15 min | **17.2 s** |
| RPO — marker row present on replica at cutover | <= 5 min lag | **replicated (0 loss for marker)** |
| Time to restore primary serving | — | **17.7 s** |

Baseline serving region: `swedencentral`. Marker: `drill-marker-20260703102835` at 10:28:35.

## 5-Whys (worst observed gap)
1. Why any user-visible delay? Front Door only reroutes after N failed probes.
2. Why N probes? Load-balancing sample_size=4, successful_samples_required=3.
3. Why those values? Balance between flap-resistance and fast failover.
4. Why not lower? Too-aggressive probing flaps on transient blips.
5. Root: RTO is bounded by (probe interval x samples). Tunable in the origin group.

## Actions
- [ ] If RTO must drop, lower `interval_in_seconds` and `sample_size` in the origin group.
- [ ] Automate replica promotion (promote-replica.sh) into the runbook for a real outage.

## PITR restore drill

Ran a point-in-time restore of the primary (`pg-s3-dev-sdc-001`) to a new server
`pg-s3-restore-drill` at restore point `2026-07-03T07:27:27Z`. The restored copy
came up **Ready** and contained **exactly 1 `notes` row** — the state *at that
instant* (before note id=2 was written at 07:28:18). This confirms Postgres
Flexible automated backups + PITR work and are point-accurate. Temp server
deleted afterward.

## Replica promotion

Promoted the DR replica `pg-s3-dev-plc-001` to a standalone read-write server
(`promote-replica.sh`), then cleared `readOnly` on the DR app — a `POST` to the
DR region returned `201`, proving the DR site can take writes after failover.
(Irreversible: the replication link ends on promotion.)
